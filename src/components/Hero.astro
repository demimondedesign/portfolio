---
import Logo from './Logo.astro';

interface Props {
	title: string;
	tagline?: string;
	align?: 'start' | 'center';
}

const { align = 'center', tagline, title } = Astro.props;
---


<header class:list={['hero w-full bg-center bg-repeat', align]}>
	<div class="flex items-center justify-center w-full h-full bg-gray-900 bg-opacity-50">
		<div class="text-center">
				<div class="py-2">
					<h1 class="title hidden">{title}</h1>
					<Logo />
					<!-- <div class="logo w-full bg-center bg-cover"></div> -->
					<!-- <img src="/assets/logo-sm.svg" alt="Demimonde Designs" class="mx-auto w-full" /> -->
				</div>
				<div class="py-2 responsive-px">
					{tagline && <p class="tagline">{tagline}</p>}
				</div>
		</div>
</div>
	<slot />
</header>


<style lang="scss">
	@import '../styles/sass-variables';

	.hero {
		position: relative;
		// background-image: url('/assets/backgrounds/header-background.png');
		background-image: url('/assets/backgrounds/background-tile.jpg');
		height: 30rem;
		.logo {
			height: 20rem;
			background-image: url('/assets/logo-sm.svg');
		}
	}

	.hero .responsive-px {
		padding-left: 2.5rem;
		padding-right: 2.5rem;
	}

	.hero .text-align-center {
		text-align: center !important;
		margin: auto;
	}

	.title,
	.tagline {
		color: #af9c6d;
		margin-inline: auto;
	}

	.title {
		font-size: var(--text-2xl);
		color: #fefefe;
	}

	@media (min-width: $breakpoint-mobile) {
		.hero {
			font-size: var(--text-lg);
			.logo {
				height: 20rem;
				background-image: url('/assets/logo.svg');
			}
		}

		.hero .responsive-px {
			padding-left: 2rem 2rem;
			padding-right: 2rem 2rem;
		}
		
		.tagline {
			font-size: var(--text-lg);
		}
		.start {
			text-align: start;
		}

		.start .title,
		.start .tagline {
			margin-inline: unset;
		}

		.title {
			font-size: var(--text-4xl);
		}
	}
</style>
